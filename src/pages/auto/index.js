import React, { ReactFragment } from 'react';
import PropTypes from 'prop-types';

const index = props => {
    return (
        <ReactFragment>
            <span>13345</span>
        </ReactFragment>
    );
};

index.propTypes = {
    
};

export default index;