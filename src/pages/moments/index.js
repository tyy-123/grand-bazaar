import React, { ReactFragment } from 'react';

const Moments = props => {
    return (
        <ReactFragment>
            <span>13345</span>
        </ReactFragment>
    );
};

export default Moments;